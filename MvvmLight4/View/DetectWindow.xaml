<Controls:MetroWindow x:Class="MvvmLight4.View.DetectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:MvvmLight4.View"
        xmlns:mvvm="http://www.galasoft.ch/mvvmlight"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource Locator},Path=Detect}"
        Title="检测" Height="440" Width="460" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <mvvm:EventToCommand Command="{Binding LoadedCmd}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <StackPanel Margin="5">
            <StackPanel Margin="0 0 0 10">
                <StackPanel HorizontalAlignment="Left">
                    <Label FontSize="16" Content="基础设置" Width="100"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
                    <Label Content="视频选择：" Width="80"/>
                    <Button Width="250" Content="{Binding ReceiveStr}" FontSize="12" FontFamily="Microsoft YaHei UI" Command="{Binding OpenVSC}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
                    <Label Content="模型选择：" Width="80"/>
                    <ComboBox Width="250" ItemsSource="{Binding ModelList}" SelectedItem="{Binding ModelItem}" DisplayMemberPath="ModelModel.ModelName" SelectedValuePath="Id">
                    </ComboBox>
                </StackPanel>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right">
                <Button Width="80" Margin="0 0 22 0" Style="{StaticResource AccentedSquareButtonStyle}" Content="检测" Command="{Binding DetectCmd}"/>
            </StackPanel>
            <StackPanel Margin="15 10">
                <StackPanel Height="10">
                    <Canvas>
                        <ProgressBar Width="413" Height="10" Visibility="{Binding FilterProgVb}" IsIndeterminate="True"/>
                        <ProgressBar  Width="413" Height="10" Visibility="{Binding DetectProgVb}" Value="{Binding DetectProgNum}"/>
                    </Canvas>
                </StackPanel>
                <TextBox Name="LogTB" Height="200" TextChanged="LogTB_TextChanged" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding LogText}"></TextBox>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Width="80" Margin="0 0 22 0" Style="{StaticResource AccentedSquareButtonStyle}" Content="人工回溯" Command="{Binding OpenBTC}"/>
                <Button Width="80" Margin="0 0 22 0" Style="{StaticResource AccentedSquareButtonStyle}" Content="导出" Command="{Binding OpenEC}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
